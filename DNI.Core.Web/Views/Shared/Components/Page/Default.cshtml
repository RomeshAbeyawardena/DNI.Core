@model PageViewComponentViewModel

@foreach(var section in Model.Sections)
{
    if(section.IsHero)
        @await (Component.InvokeAsync<HeroViewComponent>(section))
    else
        @await (Component.InvokeAsync<SectionViewComponent>(section))
}